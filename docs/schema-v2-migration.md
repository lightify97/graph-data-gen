# Schema v2 — migration report

Generated by `python -m skygenic_scans.schema_migrate` from
`schema/ontology.yaml` (v1) and `schema/resolutions.yaml`. Do not hand-edit.

## Summary

- Active relationship rows: **86**
- Retired (kept with `status: retired` + `superseded_by`): **31**
- New rows introduced by resolutions: **14**
- Conflicts addressed: **20**

Retired rows are retained rather than deleted so a v1→v2 data migration can
map every old edge to its replacement, and so a reader who remembers a name
can find out where it went.

## Retirements by conflict

| Conflict | Retired | Superseded by |
|---|---|---|
| C-01 | `CAUSES` (BiologicalProcess→Disease) | `DRIVES_DISEASE_PATHOLOGY` |
| C-01 | `DRIVES_PATHOLOGY` (BiologicalProcess→Disease) | `DRIVES_DISEASE_PATHOLOGY` |
| C-02 | `PRECEDES` (BiologicalProcess→BiologicalProcess) | `TEMPORALLY_PRECEDES` |
| C-03 | `RESULTS_IN` (BiologicalProcess→Phenotype) | `PRODUCES_CLINICAL_PHENOTYPE` |
| C-03 | `PRODUCES_OUTCOME` (BiologicalProcess→Phenotype) | `PRODUCES_CLINICAL_PHENOTYPE` |
| C-04 | `EVOLUTIONARILY_CONSERVED_IN` (BiologicalProcess→Species) | `CONSERVED_IN` |
| C-05 | `SERVES_AS_BIOMARKER_FOR` (BiologicalProcess→Disease) | `BIOMARKER_FOR` |
| C-06 | `PART_OF` (BiologicalProcess→Pathway) | `CONSTITUTES_PATHWAY` |
| C-07 | `ACTIVATED_BY` (BiologicalProcess→Drug) | `PHARMACOLOGICALLY_ACTIVATES` |
| C-07 | `SUPPRESSED_BY` (BiologicalProcess→Drug) | `PHARMACOLOGICALLY_INHIBITS` |
| C-08 | `INVOLVES` (BiologicalProcess→Variant) | `mQTL_MODULATES` |
| C-09 | `OCCURS_IN` (BiologicalProcess→Tissue) | `OPERATES_WITHIN_CONTEXT` |
| C-09 | `CONTEXTUALIZES` (Tissue→BiologicalProcess) | `OPERATES_WITHIN_CONTEXT` |
| C-09 | `OCCURS_IN` (ProteinComplex→Tissue) | `OPERATES_WITHIN_CONTEXT` |
| C-10 | `SUPPORTED_BY` (BiologicalProcess→Publication) | `EVIDENCED_BY` |
| C-10 | `PROCESS_EVIDENCED_BY` (BiologicalProcess→Publication) | `EVIDENCED_BY` |
| C-10 | `SUPPORTED_BY` (Gene→Publication) | `EVIDENCED_BY` |
| C-10 | `SUPPORTED_BY` (Tissue→Publication) | `EVIDENCED_BY` |
| C-10 | `SUPPORTED_BY` (ProteinComplex→Publication) | `EVIDENCED_BY` |
| C-10 | `PHENOTYPICALLY_SUPPORTED_BY` (Phenotype→Publication) | `EVIDENCED_BY` |
| C-10 | `DISEASE_EVIDENCED_BY` (Disease→Publication) | `EVIDENCED_BY` |
| C-10 | `SUPPORTED_BY_PROVENANCE` (SkygenicHypothesis→Publication) | `EVIDENCED_BY` |
| C-10 | `DRUG_EVIDENCED_BY` (Drug→Publication) | `EVIDENCED_BY` |
| C-12 | `STANDARDIZES_DISEASE` (Disease→OntologyTerm) | `STANDARDIZED_BY_ONTOLOGY` |
| C-13 | `REGULATED_BY_QTL` (Gene→Variant) | `eQTL_MODULATES` |
| C-14 | `EXPRESSES` (Tissue→Gene) | `EXPRESSED_IN` |
| C-15 | `EXHIBITS_PATHOLOGY_OF` (Tissue→Disease) | `LOCALIZES_PATHOLOGY_IN` |
| C-16 | `ACCUMULATES` (Tissue→Drug) | `DISTRIBUTED_IN_CONTEXT` |
| C-17 | `CAUSALLY_DRIVES` (Variant→Disease) | `MR_VALIDATES_RISK_FOR` |
| C-18 | `AMELIORATES` (Drug→Phenotype) | `AMELIORATES_TRAIT` |
| C-19 | `TREATS_INDICATION` (Drug→Disease) | `CLINICALLY_TREATS` |

## New relationships

| Introduced by | Relationship | Purpose |
|---|---|---|
| C-09 | `OPERATES_WITHIN_CONTEXT` (ProteinComplex→Tissue) | Anatomical context for a protein complex (replaces OCCURS_IN). |
| C-10 | `EVIDENCED_BY` (Gene→Publication) | — |
| C-10 | `EVIDENCED_BY` (Protein→Publication) | — |
| C-10 | `EVIDENCED_BY` (BiologicalProcess→Publication) | — |
| C-10 | `EVIDENCED_BY` (ProteinComplex→Publication) | — |
| C-10 | `EVIDENCED_BY` (Tissue→Publication) | — |
| C-10 | `EVIDENCED_BY` (Disease→Publication) | — |
| C-10 | `EVIDENCED_BY` (Drug→Publication) | — |
| C-10 | `EVIDENCED_BY` (Phenotype→Publication) | — |
| C-10 | `EVIDENCED_BY` (Variant→Publication) | — |
| C-10 | `EVIDENCED_BY` (Pathway→Publication) | — |
| C-10 | `EVIDENCED_BY` (SkygenicHypothesis→Publication) | — |
| C-07 | `PHARMACOLOGICALLY_ACTIVATES` (Drug→BiologicalProcess) | Drug positively modulates a biological process. |
| C-07 | `PHARMACOLOGICALLY_INHIBITS` (Drug→BiologicalProcess) | Drug negatively modulates a biological process. |

## Resolutions requiring action outside the schema

### C-11 — no_change

Type reuse across endpoint pairs is legal and idiomatic in Neo4j, and these are genuinely distinct relationships that happen to share a verb. KEEP THEM. The only real problem is PRIM_P09 load_predicate_importance_table, which is keyed on predicate alone, so one weight for CAUSES must serve both "a biological process causes a disease" and "a compound causes a phenotype". That weight feeds compute_gap_importance at 20%. FIX: key the table on (predicate, from_label, to_label). Config change, not a schema change — recorded here so it is not lost.

### C-20 — clarification

Part 1 SII.1 "Remove: Gene -> Gene, Protein -> Protein, ..." is ambiguous. Reading 1 (adopted): remove true self-loops, (n)-[r]->(n). Reading 2 would delete UPREGULATES, DOWNREGULATES, INTERACTS_WITH, PREDICTED_TO_INTERACT_WITH, ORTHOLOG_OF, TEMPORALLY_PRECEDES, MECHANISTICALLY_SIMILAR_TO, ONTOLOGICALLY_INCLUDES and SEMANTICALLY_OVERLAPS_WITH — all of which Part 2 explicitly defines and SCAN-12/14/25 depend on. The doc's own justification ("a compound does not therapeutically interact with itself") supports only reading 1. v2 states it as an explicit schema constraint.

## Migrating loaded data

```cypher
// Example: C-19. Repoint TREATS_INDICATION onto CLINICALLY_TREATS,
// preserving all evidence properties, then drop the duplicate.
MATCH (d:Drug)-[old:TREATS_INDICATION]->(x:Disease)
MERGE (d)-[new:CLINICALLY_TREATS]->(x)
ON CREATE SET new = properties(old)
DELETE old
```

Order matters for the inverse merges (C-08/13/14/15/16): reverse the
endpoints when repointing, since the retired edge runs the other way.

```cypher
// Example: C-14. EXPRESSES(Tissue→Gene) becomes EXPRESSED_IN(Gene→Tissue).
MATCH (t:Tissue)-[old:EXPRESSES]->(g:Gene)
MERGE (g)-[new:EXPRESSED_IN]->(t)
ON CREATE SET new = properties(old)
DELETE old
```
